import 'package:dio/dio.dart';

/// Web stub: the browser handles address selection (Happy Eyeballs), so there
/// is nothing to do and dart:io is unavailable anyway.
void applyIpv4PreferenceImpl(Dio dio) {}
